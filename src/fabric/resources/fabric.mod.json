{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": [
    "${author}"
  ],
  "contact": {
    "repo": "${source_url}"
  },
  "license": "${license}",
  "icon": "icon.png",
  "environment": "*",
  "mixins": [
    "potatoptimize.vanilla.mixins.json"
  ],
  "depends": {
    "fabricloader": "*",
    "minecraft": ">=${min_minecraft_version}"
  }
}
